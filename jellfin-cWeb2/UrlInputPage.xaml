<Page x:Class="jellyfin_cWeb2.UrlInputPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:jellyfin_cWeb2"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">



    <Grid Background="{StaticResource Color0}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Background="{StaticResource Color10}">
            <Image x:Name="logo" Source="/Assets/OnBoardingLogo.png" Stretch="Uniform" HorizontalAlignment="Center" Height="80" Margin="10" />
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="TxtTitle" Text="Enter your Jellyfin Server URL" Foreground="{StaticResource Color100}" FontSize="20"  FontFamily="{StaticResource JellyfinFamilyFont}" HorizontalAlignment="Center"/>
            <TextBox x:Name="UrlInputBox" Style="{StaticResource PrimaryTextBox}" Foreground="{StaticResource Color10}" FontSize="{StaticResource FontL}" 
                         PlaceholderText="http://example.com or 192.168.100.102:8086" KeyDown="UrlInputBox_KeyDown"/>
                <Button x:Name="BtnConnect" Content="Connect" Click="SubmitButton_Click" Margin="7" HorizontalAlignment="Center" Style="{StaticResource PrimaryButton}"/>
                <Button x:Name="BtnConfirm" Content="Confirm Connection" Click="ConfirmButton_Click" Style="{StaticResource PrimaryButton}" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2" Padding="10">
            <TextBlock x:Name="TxtError" Text="Error" Visibility="Collapsed" Foreground="Red" FontFamily="{StaticResource JellyfinFamilyFont}" Padding="10" HorizontalAlignment="Center" TextWrapping="Wrap"/>
        </Grid>
    </Grid>
</Page>